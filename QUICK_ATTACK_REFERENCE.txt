╔══════════════════════════════════════════════════════════════════════════════╗
║                    ESP32-CAM ATTACK QUICK REFERENCE                          ║
║                         Target: 10.42.0.103                                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ SETUP (Run these first)                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

1. Start e-raksha:
   cd /home/sajal/Desktop/Hackathons/e-raksha
   sudo python main.py

2. Open monitoring:
   ./start_monitoring.sh

3. Open dashboard:
   firefox http://10.42.0.1:5000


┌──────────────────────────────────────────────────────────────────────────────┐
│ ATTACK 1: Port Scanning → Response: RATE_LIMIT (L2)                         │
└──────────────────────────────────────────────────────────────────────────────┘

Basic scan:
  nmap 10.42.0.103

Aggressive scan (better detection):
  nmap -T4 10.42.0.103

Full port scan (guaranteed detection):
  nmap -p- 10.42.0.103

✓ Expected: Port scan detected in <5 seconds


┌──────────────────────────────────────────────────────────────────────────────┐
│ ATTACK 2: HTTP Enumeration → Response: DEPLOY_HONEYPOT (L3)                 │
└──────────────────────────────────────────────────────────────────────────────┘

Rapid requests (20 times):
  for i in {1..20}; do curl -s http://10.42.0.103/ > /dev/null; echo "Request $i"; sleep 0.3; done

Directory brute force:
  for path in / /admin /config /stream /api /debug; do
    curl -v http://10.42.0.103$path
    sleep 0.5
  done

✓ Expected: High request rate detected, honeypot deployed


┌──────────────────────────────────────────────────────────────────────────────┐
│ ATTACK 3: Unauthorized Stream Access → Response: QUARANTINE (L4)            │
└──────────────────────────────────────────────────────────────────────────────┘

Access stream 15 times (triggers repeat offender):
  for i in {1..15}; do
    timeout 2s curl http://10.42.0.103/stream -o /dev/null 2>&1
    echo "Stream access $i"
    sleep 0.5
  done

✓ Expected: Repeat offender detected, device quarantined


┌──────────────────────────────────────────────────────────────────────────────┐
│ ATTACK 4: Command Injection → Response: ISOLATE (L5) 🔒                     │
└──────────────────────────────────────────────────────────────────────────────┘

SQL injection:
  curl "http://10.42.0.103/?id=1' OR '1'='1"

Command injection:
  curl "http://10.42.0.103/?cmd=;ls"
  curl "http://10.42.0.103/stream?debug=\`whoami\`"
  curl "http://10.42.0.103/config?action=\$(reboot)"

Path traversal:
  curl "http://10.42.0.103/../../etc/passwd"

✓ Expected: IDS detects malicious payload, device ISOLATED


┌──────────────────────────────────────────────────────────────────────────────┐
│ VERIFICATION                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

Check if camera is isolated:
  curl http://10.42.0.103 --max-time 5
  → Should timeout or refuse connection

Check iptables rules:
  sudo iptables -L -v -n | grep 10.42.0.103
  → Should show DROP rules

Check device status in database:
  sqlite3 data/rakshak.db "SELECT ip, status, risk_score FROM devices WHERE ip='10.42.0.103'"
  → Status should be: isolated

Check threats detected:
  sqlite3 data/rakshak.db "SELECT type, severity FROM threats ORDER BY timestamp DESC LIMIT 5"
  → Should show: port_scan, high_request_rate, command_injection, etc.


┌──────────────────────────────────────────────────────────────────────────────┐
│ RECOVERY (After Testing)                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

Unisolate camera via dashboard:
  1. Go to http://10.42.0.1:5000
  2. Click "Devices" tab
  3. Find 10.42.0.103
  4. Click "Unisolate" button

Or via command line:
  sudo iptables -D FORWARD -s 10.42.0.103 -j DROP
  sudo iptables -D FORWARD -d 10.42.0.103 -j DROP

Verify recovery:
  curl http://10.42.0.103
  → Should work again


┌──────────────────────────────────────────────────────────────────────────────┐
│ COMPLETE ATTACK SEQUENCE (All-in-One)                                       │
└──────────────────────────────────────────────────────────────────────────────┘

Run these in order with 10-15 second pauses:

#!/bin/bash
TARGET="10.42.0.103"

# Phase 1: Recon
nmap -T4 $TARGET
sleep 15

# Phase 2: Enumeration
for i in {1..20}; do curl -s http://$TARGET/ > /dev/null; sleep 0.3; done
sleep 15

# Phase 3: Unauthorized Access
for i in {1..15}; do timeout 2s curl http://$TARGET/stream -o /dev/null 2>&1; sleep 0.5; done
sleep 15

# Phase 4: Exploitation
curl "http://$TARGET/?cmd=;ls"
curl "http://$TARGET/stream?debug=\`whoami\`"
sleep 15

# Phase 5: Verify Isolation
ping -c 3 $TARGET || echo "✓ Camera isolated successfully!"


┌──────────────────────────────────────────────────────────────────────────────┐
│ EXPECTED TIMELINE                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

T+0:00  → Start attacks
T+0:05  → Port scan DETECTED → RATE_LIMIT (L2)
T+0:20  → HTTP enum DETECTED → DEPLOY_HONEYPOT (L3)
T+0:40  → Repeat offender DETECTED → QUARANTINE (L4)
T+1:00  → Exploit DETECTED → ISOLATE (L5) 🔒
T+1:15  → Camera fully isolated from network ✓


┌──────────────────────────────────────────────────────────────────────────────┐
│ MONITORING TIPS                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

• Keep dashboard open in browser (auto-refreshes)
• Watch the monitoring terminals for real-time detection
• Take screenshots before/after each attack phase
• Check logs between attacks to see response escalation
• Space attacks 10-15 seconds apart for clear detection


┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

"Nothing detected":
  → Check e-raksha is running: ps aux | grep "python.*main.py"
  → Verify gateway mode: grep "GATEWAY MODE" data/logs/rakshak.log
  → Check packet filter: sudo iptables -L -v -n | grep NFQUEUE

"Device not isolated":
  → Check logs: tail -f data/logs/rakshak.log | grep ISOLATE
  → Verify iptables: sudo iptables -L -v -n | grep 10.42.0.103
  → Manual isolation: See dashboard or REAL_ATTACK_GUIDE.md

"Camera shows as unknown":
  → Check MAC: arp -a | grep 10.42.0.103
  → Database update: See TESTING_ESP32CAM.md section "Issue: ESP32-CAM Shows as unknown"


═══════════════════════════════════════════════════════════════════════════════
For detailed explanations, see:
  • REAL_ATTACK_GUIDE.md - Complete attack scenarios
  • TESTING_ESP32CAM.md - Full testing procedures
═══════════════════════════════════════════════════════════════════════════════
